import { PrismaClient as PC } from '@prisma/client'
import { PrismaClient as PCA } from '../../generated/database/client'

it('should work', () => {
  expect(1).toBe(1)
})

// TODO Test for correct engine file here as well