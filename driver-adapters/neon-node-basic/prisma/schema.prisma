generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // fake url to ensure the driver adapter is used
  url      = "postgresql://foo"
}

model User {
  id    String  @default(cuid()) @id
  email String  @unique
  name  String?
  age   Int?
}
