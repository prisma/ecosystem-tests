generator client {
  provider = "prisma-client"
  output = "../generated"
}

datasource db {
  provider = "mysql"
}

model User {
  id    String  @default(cuid()) @id
  email String  @unique
  name  String?
  age   Int?
}
