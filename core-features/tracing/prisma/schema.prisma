generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["tracing"]
}

datasource db {
    provider = "postgresql"
    url      = "DATABASE_URL"
}

model User {
    id    String  @id @default(cuid())
    email String  @unique
    name  String?
}
