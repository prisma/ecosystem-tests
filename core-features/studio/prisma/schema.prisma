datasource db {
    provider = "sqlite"
    url      = "file:./dev.db"
}

model User {
    id    Int    @id
    name  String
    email String

    parentId Int
    parent   User   @relation(fields: [parentId], references: [id])
    children User[] @relation("UserToUser")
}
