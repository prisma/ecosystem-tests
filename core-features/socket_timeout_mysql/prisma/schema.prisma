generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = "mysql://this-is-not-going-to-be-used"
}

model User {
    id    String  @default(cuid()) @id
    email String  @unique
    name  String?
}
