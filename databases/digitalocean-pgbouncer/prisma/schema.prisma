datasource db {
  provider = "postgresql"
  url      = env("DATABASE_DO_PG_BOUNCER_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  email String @default("")
  id    String @id
  name  String @default("")]
}
