datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output = "../generated"
}

model User {
  email String @default("")
  id    String @id
  name  String @default("")
}
