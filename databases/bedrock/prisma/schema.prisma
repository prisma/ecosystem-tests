generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:root@127.0.0.1:3306/bedrock"
}

model User {
  id    String  @default(cuid()) @id
  email String  @unique
  name  String?
}
