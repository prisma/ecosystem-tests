generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./bedrock.db"
}

model User {
  id    String  @default(cuid()) @id
  email String  @unique
  name  String?
}
