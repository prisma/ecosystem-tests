datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "../src/custom-prisma"
}

model User {
  id   Int     @id @default(autoincrement())
  name String?
}
