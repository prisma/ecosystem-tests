datasource db {
  provider = "postgresql"
  url      = env("PARCEL_PG_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id    String  @default(cuid()) @id
  email String  @unique
  name  String?
}
