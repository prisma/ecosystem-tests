datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output = "../functions/generated" // Why does this need/use a custom output?
}

model User {
  id    String  @id @default(cuid())
  email String  @unique
  name  String?
}
