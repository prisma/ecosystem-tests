generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "mysql://root:@127.0.0.1:3306/e2e-tests"
}

model User {
  id    String  @default(cuid()) @id
  email String  @unique
  name  String?
  age   Int?
}
