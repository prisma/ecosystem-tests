datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output = "../api/generated"
}

model User {
  id    String  @default(cuid()) @id
  email String  @unique
  name  String?
}
