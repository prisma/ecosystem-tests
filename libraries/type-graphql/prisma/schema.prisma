datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output = "../generated"
}

model User {
  id    Int     @default(autoincrement()) @id
  email String  @unique
  name  String?
}
