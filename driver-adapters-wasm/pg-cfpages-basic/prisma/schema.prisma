generator client {
  provider = "prisma-client"
  output = "../fns/generated"
}

datasource db {
  provider = "postgresql"
}

model User {
  id    String  @default(cuid()) @id
  email String  @unique
  name  String?
  age   Int?
}
